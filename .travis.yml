language: python
python:
  - "3.5"
  - "3.6"

install:
  - pip install coveralls
  - pip install pylint pydocstyle pytest-cov

script:
  - pytest --cov=pyfn tests/
  - pydocstyle pyfn
  - pylint --rcfile .pylintrc pyfn

after_success:
  - coveralls
