language: python
python:
  - "3.5"
  - "3.6"

install:
  - pip install coveralls
  - pip install pydocstyle pytest-cov

script:
  - python -m pytest --cov=pyfn tests/
  - pylint pyfn
  - pydocstyle pyfn

after_success:
  - coveralls
