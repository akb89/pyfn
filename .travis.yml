language: python
python:
  - "3.5"
  - "3.6"

install:
  - pip install coveralls
  - pip install pylint pydocstyle pycodestyle pytest-cov

script:
  - python -m pytest --cov=pyfn tests/
  - pydocstyle pyfn
  - pycodestyle pyfn
  - pylint --rcfile .pylintrc pyfn

after_success:
  - coveralls
